# Metadonnees Codex CLI pour le skill 'rivett'.
# Miroir du frontmatter de SKILL.md : les deux doivent rester en phase.
# Utilise uniquement quand le skill est installe dans $CODEX_HOME/skills/.
# Claude Code lit SKILL.md directement.

name: rivett
description: |
  A utiliser pour toute operation RiveTT — automatisation d'une maquette
  Revit 2026.5+ ou 2027, workflows d'outils MCP, developpement du plugin ou
  du serveur C#. Couvre les ecritures sures, l'escalade vers
  send_code_to_revit, l'IFC, la creation d'outils, l'audit de securite et
  les controles de build .NET 10.

references_dir: references
entrypoint: SKILL.md
