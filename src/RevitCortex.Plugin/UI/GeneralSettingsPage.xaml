<Page x:Class="RevitCortex.Plugin.UI.GeneralSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="550">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="General Settings" FontSize="22" FontWeight="SemiBold" Foreground="#333"/>
            <TextBlock Text="Configure server and plugin options" Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Connection Status Banner -->
        <Border Grid.Row="1" x:Name="StatusBanner" CornerRadius="6" Padding="14,10"
                Margin="0,0,0,15" Background="#F5F5F5" BorderThickness="1" BorderBrush="#E0E0E0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Status dot -->
                <Ellipse x:Name="StatusDot" Grid.Column="0" Width="12" Height="12"
                         VerticalAlignment="Center" Margin="0,0,10,0" Fill="#9E9E9E"/>
                <!-- Status text -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusTitle" Text="Server stopped"
                               FontSize="13" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock x:Name="StatusDetail" Text="Click 'Cortex Switch' in the ribbon to start"
                               FontSize="11" Foreground="#666" Margin="0,2,0,0"/>
                </StackPanel>
                <!-- Port badge -->
                <Border Grid.Column="2" x:Name="PortBadge" CornerRadius="4" Padding="8,3"
                        Background="#E0E0E0" VerticalAlignment="Center">
                    <TextBlock x:Name="PortBadgeText" FontSize="11" Foreground="#555"/>
                </Border>
            </Grid>
        </Border>

        <!-- Update Available Banner (hidden until UpdateChecker finds a newer version) -->
        <Border Grid.Row="2" x:Name="UpdateBanner" CornerRadius="6" Padding="14,10"
                Margin="0,0,0,15" Background="#FFF8E1" BorderThickness="1" BorderBrush="#FFB300"
                Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock x:Name="UpdateTitle" FontSize="13" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock x:Name="UpdateDetail" FontSize="11" Foreground="#666"
                               Margin="0,2,0,0" TextWrapping="Wrap"/>
                    <!-- Progress bar: visible only while downloading -->
                    <Grid x:Name="UpdateProgressGrid" Visibility="Collapsed" Margin="0,6,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ProgressBar x:Name="UpdateProgress" Grid.Row="0"
                                     Height="5" Minimum="0" Maximum="100" Value="0"
                                     Background="#E0E0E0" Foreground="#1976D2"/>
                        <TextBlock x:Name="UpdateProgressText" Grid.Row="1"
                                   FontSize="10" Foreground="#888" Margin="0,3,0,0"/>
                    </Grid>
                </StackPanel>
                <!-- Action buttons — only one visible at a time (except ERROR shows both) -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <Button x:Name="UpdateActionButton"
                            Content="Download &amp; Install"
                            Padding="14,6" FontSize="13"
                            Background="#FFB300" Foreground="White" BorderBrush="#FF8F00"
                            Click="UpdateAction_Click" Cursor="Hand"/>
                    <!-- Secondary button: visible only in ERROR state, to the right of "Riprova" -->
                    <Button x:Name="UpdateManualButton"
                            Content="Scarica manualmente"
                            Padding="10,6" Margin="8,0,0,0" FontSize="12"
                            Background="#9E9E9E" Foreground="White" BorderBrush="#757575"
                            Click="UpdateManual_Click" Cursor="Hand" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Form -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <Grid Margin="0,0,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Port -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,10" VerticalAlignment="Center">
                    <TextBlock Text="Server Port" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock Text="TCP port (requires restart)" FontSize="11" Foreground="#666" Margin="0,3,0,0"/>
                </StackPanel>
                <TextBox x:Name="PortTextBox" Grid.Row="0" Grid.Column="1"
                         Margin="15,10,0,10" Padding="8,6" FontSize="14"
                         VerticalAlignment="Center" Width="120" HorizontalAlignment="Left"
                         BorderBrush="#E0E0E0"/>

                <Separator Grid.Row="1" Grid.ColumnSpan="2" Margin="0,5" Background="#E0E0E0"/>

                <!-- Log Level -->
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,10" VerticalAlignment="Center">
                    <TextBlock Text="Log Level" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock Text="Verbosity of plugin logging" FontSize="11" Foreground="#666" Margin="0,3,0,0"/>
                </StackPanel>
                <ComboBox x:Name="LogLevelComboBox" Grid.Row="2" Grid.Column="1"
                          Margin="15,10,0,10" Padding="8,6" FontSize="14"
                          VerticalAlignment="Center" Width="160" HorizontalAlignment="Left"
                          BorderBrush="#E0E0E0">
                    <ComboBoxItem Content="Debug"/>
                    <ComboBoxItem Content="Info"/>
                    <ComboBoxItem Content="Warning"/>
                    <ComboBoxItem Content="Error"/>
                </ComboBox>

                <Separator Grid.Row="3" Grid.ColumnSpan="2" Margin="0,5" Background="#E0E0E0"/>

                <!-- Read-Only Mode -->
                <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,10" VerticalAlignment="Center">
                    <TextBlock Text="Read-Only Mode" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock Text="Block all write operations" FontSize="11" Foreground="#666" Margin="0,3,0,0"/>
                </StackPanel>
                <CheckBox x:Name="ReadOnlyCheckBox" Grid.Row="4" Grid.Column="1"
                          Margin="15,10,0,10" VerticalAlignment="Center"
                          Content="Enable read-only mode" FontSize="13" Foreground="#333"/>

                <Separator Grid.Row="5" Grid.ColumnSpan="2" Margin="0,5" Background="#E0E0E0"/>

                <!-- Error Telemetry -->
                <StackPanel Grid.Row="6" Grid.Column="0" Margin="0,10" VerticalAlignment="Center">
                    <TextBlock x:Name="TelemetryTitle" Text="Error Telemetry" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock x:Name="TelemetrySubtitle" Text="Pseudonymous, opt-in, no model data" FontSize="11" Foreground="#666" Margin="0,3,0,0"/>
                </StackPanel>
                <CheckBox x:Name="EnableTelemetryCheckBox" Grid.Row="6" Grid.Column="1"
                          Margin="15,10,0,10" VerticalAlignment="Center" FontSize="13" Foreground="#333"/>

                <Separator Grid.Row="7" Grid.ColumnSpan="2" Margin="0,5" Background="#E0E0E0"/>

                <!-- Support Reports -->
                <StackPanel Grid.Row="8" Grid.Column="0" Margin="0,10" VerticalAlignment="Center">
                    <TextBlock x:Name="SupportReportsTitle" Text="Support Reports" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock x:Name="SupportReportsSubtitle" Text="Number of reports to keep on disk" FontSize="11" Foreground="#666" Margin="0,3,0,0"/>
                </StackPanel>
                <StackPanel Grid.Row="8" Grid.Column="1" Orientation="Horizontal" Margin="15,10,0,10" VerticalAlignment="Center">
                    <TextBox x:Name="KeepCountTextBox" Padding="8,6" FontSize="14"
                             Width="70" BorderBrush="#E0E0E0"/>
                    <Button x:Name="OpenReportsFolderButton" Content="Open folder"
                            Padding="12,6" Margin="15,0,0,0" FontSize="13"
                            Background="#F5F5F5" Foreground="#333" BorderBrush="#E0E0E0"
                            Click="OpenReportsFolder_Click" Cursor="Hand"/>
                    <Button x:Name="DeleteAllReportsButton" Content="Delete all now"
                            Padding="12,6" Margin="10,0,0,0" FontSize="13"
                            Background="#F5F5F5" Foreground="#333" BorderBrush="#E0E0E0"
                            Click="DeleteAllReports_Click" Cursor="Hand"/>
                </StackPanel>

                <Separator Grid.Row="9" Grid.ColumnSpan="2" Margin="0,5" Background="#E0E0E0"/>

                <!-- Plugin Version -->
                <StackPanel Grid.Row="10" Grid.Column="0" Margin="0,10" VerticalAlignment="Center">
                    <TextBlock Text="Plugin Version" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                </StackPanel>
                <TextBlock x:Name="PluginVersionText" Grid.Row="10" Grid.Column="1"
                           Margin="15,10,0,10" FontSize="14" Foreground="#333" VerticalAlignment="Center"/>

                <Separator Grid.Row="11" Grid.ColumnSpan="2" Margin="0,5" Background="#E0E0E0"/>

                <!-- Revit Version -->
                <StackPanel Grid.Row="12" Grid.Column="0" Margin="0,10" VerticalAlignment="Center">
                    <TextBlock Text="Revit Version" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                </StackPanel>
                <TextBlock x:Name="RevitVersionText" Grid.Row="12" Grid.Column="1"
                           Margin="15,10,0,10" FontSize="14" Foreground="#333" VerticalAlignment="Center"/>
            </Grid>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock x:Name="SaveFeedbackText" VerticalAlignment="Center" Margin="0,0,15,0"
                       FontSize="13" FontWeight="SemiBold" Visibility="Collapsed"/>
            <Button Content="Reset Defaults" Padding="15,8" Margin="0,0,10,0" FontSize="13"
                    Background="#F5F5F5" Foreground="#333" BorderBrush="#E0E0E0"
                    Click="ResetDefaults_Click" Cursor="Hand"/>
            <Button Content="Save" Padding="20,8" FontSize="13"
                    Background="#00838F" Foreground="White" BorderBrush="#006064"
                    Click="Save_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Page>
